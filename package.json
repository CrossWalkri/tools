{
  "name": "cross-walkri-tools",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for CROSS+WALKRI tooling infrastructure",
  "license": "CC0-1.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r --parallel exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
