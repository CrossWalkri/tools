[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm && pnpm install && pnpm build && pnpm bundle"

[deploy]
startCommand = "node server-http.mjs"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
