{
  "name": "@proof-of-coord/evidence-core",
  "version": "0.4.0",
  "description": "Types, schemas, and audit logic for the evidence integrity standards: CROSS, WALKRI, ORE, and the finding contract",
  "license": "CC0-1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm generate && tsc",
    "typecheck": "tsc --noEmit",
    "generate": "node ../../scripts/generate-primitives.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}
